---
description: Target a git worktree - /worktree <branch> or off
---

Call the `worktree` tool with target set to: $ARGUMENTS

If no arguments provided, call `worktree` with no target to show current status.
